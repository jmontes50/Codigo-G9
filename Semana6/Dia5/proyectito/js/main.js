/**
 * 1. Creen un arreglo de objetos, que cada objeto se una bebida o postre, minimo 4 productos
 * 1.1 el objeto requeria los siguientes campos
 * -id
 * -nombre
 * -descripcion
 * -precio
 * 2. Mostrar el arreglo convertido en el html, de la misma manera que se hizo con los platos
 * 3. escuchar el evento de los botones y agregarlo al carrito
 * //HAGANLO DE 0
 */